{% extends "base.twig" %}

{% block title %}{{ titulo | default('Lista de Antenas') }}{% endblock %}

{% block content %}
    <div class="container py-4 bg-white">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="mb-0 color-1">{{ titulo | default('Lista de Antenas') }}</h2>
            <a href="{{ BASE_URL }}/antena/cadastrar" class="btn btn-primary btn-loading">
                <i class="bi bi-plus-circle"></i> Nova Antena
            </a>
        </div>

{#        #}{# ALERTAS DE ERRO / SUCESSO (opcional) #}
{#        {% if flash_success %}#}
{#            <div class="alert alert-success">{{ flash_success }}</div>#}
{#        {% endif %}#}
{#        {% if flash_error %}#}
{#            <div class="alert alert-danger">{{ flash_error }}</div>#}
{#        {% endif %}#}
{#        {% if errors is defined and errors %}#}
{#            <div class="alert alert-danger mb-3">#}
{#                <strong>Corrija os campos abaixo:</strong>#}
{#                <ul class="mb-0">#}
{#                    {% for e in errors %}#}
{#                        <li>{{ e }}</li>#}
{#                    {% endfor %}#}
{#                </ul>#}
{#            </div>#}
{#        {% endif %}#}

        {# Ranking Top 5 UFs #}
        <div class="card mt-5 mb-3">
            <div class="card-header p-0">
                <button class="btn btn-link w-100 text-start px-3 py-2 d-flex align-items-center color-3"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#rankingUfs"
                        aria-expanded="true"
                        aria-controls="rankingUfs">
                    <strong>Estados com maior incidência de antenas</strong>
                    <span class="ms-auto toggle-caret" aria-hidden="true">▼</span>
                </button>
            </div>

            <div id="rankingUfs" class="collapse {{ show_rank }}">
                <ul class="list-group list-group-flush">
                    {% if ranking_ufs is empty %}
                        <li class="list-group-item text-muted">Sem dados para exibir.</li>
                    {% else %}
                        {% for r in ranking_ufs %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <div class="d-flex align-items-center">
                                    <span class="badge bg-secondary me-2">{{ loop.index }}</span>
                                    <strong class="ms-5 w-uf">{{ r.uf }}</strong>
                                    <span class="ms-5 text-muted">{{ r.uf_descricao }}</span>
                                </div>
                                <div class="d-flex align-items-center gap-2">
                                    <span class="badge bg-dark ms-5">{{ r.total }}</span>
                                    <a class="btn btn-sm btn-outline-primary ms-5 btn-loading"
                                       href="{{ BASE_URL }}/antena?uf={{ r.uf }}{% if q %}&q={{ q }}{% endif %}">
                                        Listar
                                    </a>
                                </div>
                            </li>
                        {% endfor %}
                    {% endif %}
                </ul>
            </div>
        </div>

        <form id="frm_filtro" method="get" class="row g-2 mt-5 mb-3">
            <div class="col-md-4">
                <input type="text" name="q" value="{{ q|default('') }}" class="form-control" placeholder="Buscar descrição...">
            </div>
            <div class="col-md-2">
                <input type="text" name="uf" value="{{ uf|default('') }}" class="form-control" placeholder="UF">
            </div>
            <div class="col-md-2 d-flex gap-2">
                <button type="submit" class="btn btn-outline-success w-50 flex-fill">
                    <i class="bi bi-search"></i> Filtrar
                </button>
                <button type="button" class="btn btn-outline-secondary w-50 flex-fill btn-loading" data-action="{{ BASE_URL }}/antena" data-ref="">
                    <i class="bi bi-search"></i> Limpar
                </button>
            </div>
        </form>

        {% if antenas is empty %}
            <div class="alert alert-warning text-center">
                Nenhuma antena encontrada.
            </div>
        {% else %}
            <p class="text-muted small text-end">
                Mostrando {{ antenas|length }} de {{ total }} registros | Página {{ page }} de {{ pages }}
            </p>
            {{ a.data_implantacao }}
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-light">
                    <tr>
                        <th>Descrição</th>
                        <th>Latitude</th>
                        <th>Longitude</th>
                        <th>UF</th>
                        <th>Altura</th>
                        <th>Data Implantação</th>
                        <th class="text-center">Ações</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for a in antenas %}
                        <tr>

                            <td>{{ a.descricao }}</td>
                            <td>{{ a.latitude|number_format(7, ',', '.') }}</td>
                            <td>{{ a.longitude|number_format(7, ',', '.') }}</td>
                            <td>{{ a.uf }}</td>
                            <td>{{ a.altura|number_format(2, ',', '.') }}</td>
                            <td>{{ a.data_implantacao ? a.data_implantacao | date("d/m/Y") : '' }}</td>
                            <td class="text-center td-actions">
                                <a href="#" class="btn btn-sm btn-outline-info btn-loading ms-2" data-action="{{ BASE_URL }}/antena/ver" data-ref="{{ a.id_antena }}">Ver</a>
                                <a href="#" class="btn btn-sm btn-outline-primary btn-mapa ms-2" data-action="{{ BASE_URL }}/antena/mapa" data-ref="{{ a.id_antena }}">Mapa</a>
                                <a href="#" class="btn btn-sm btn-outline-warning btn-loading ms-2" data-action="{{ BASE_URL }}/antena/editar" data-ref="{{ a.id_antena }}">Editar</a>
                                <a href="#" class="btn btn-sm btn-outline-danger btn-excluir ms-2" data-action="{{ BASE_URL }}/antena/excluir" data-ref="{{ a.id_antena }}">Excluir</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>

            {# Paginação #}
            {% if pages > 1 %}
                {% set start = page - 5 %}
                {% set end   = page + 4 %}

                {# Corrige limites — não deixar sair do intervalo #}
                {% if start < 1 %}
                    {% set start = 1 %}
                    {% set end = 10 %}
                {% endif %}
                {% if end > pages %}
                    {% set end = pages %}
                    {% if end - 9 > 0 %}
                        {% set start = end - 9 %}
                    {% endif %}
                {% endif %}

                <nav aria-label="Navegação de páginas">
                    <ul class="pagination justify-content-center mt-4">

                        {# Botão primeiro #}
                        <li class="page-item {{ page == 1 ? 'disabled' : '' }}">
                            <a class="page-link" href="?page=1{% if q %}&q={{ q }}{% endif %}{% if uf %}&uf={{ uf }}{% endif %}{% if show_rank  == 'show' %}&rank{% endif %}" aria-label="Primeiro">
                                <span aria-hidden="true">&lt;&lt;</span>
                            </a>
                        </li>

                        {# Botão anterior #}
                        <li class="page-item {{ page == 1 ? 'disabled' : '' }}">
                            <a class="page-link" href="?page={{ page - 1 }}{% if q %}&q={{ q }}{% endif %}{% if uf %}&uf={{ uf }}{% endif %}{% if show_rank  == 'show' %}&rank{% endif %}" aria-label="Anterior">
                                <span aria-hidden="true">&lt;</span>
                            </a>
                        </li>

                        {# Exibe as páginas dentro da janela calculada #}
                        {% for p in start..end %}
                            <li class="page-item {{ p == page ? 'active' : '' }}">
                                <a class="page-link" href="?page={{ p }}{% if q %}&q={{ q }}{% endif %}{% if uf %}&uf={{ uf }}{% endif %}{% if show_rank  == 'show' %}&rank{% endif %}">
                                    {{ p }}
                                </a>
                            </li>
                        {% endfor %}

                        {# Botão próximo #}
                        <li class="page-item {{ page == pages ? 'disabled' : '' }}">
                            <a class="page-link" href="?page={{ page + 1 }}{% if q %}&q={{ q }}{% endif %}{% if uf %}&uf={{ uf }}{% endif %}{% if show_rank  == 'show' %}&rank{% endif %}" aria-label="Próximo">
                                <span aria-hidden="true">&gt;</span>
                            </a>
                        </li>

                        {# Botão último #}
                        <li class="page-item {{ page == pages ? 'disabled' : '' }}">
                            <a class="page-link" href="?page={{ pages }}{% if q %}&q={{ q }}{% endif %}{% if uf %}&uf={{ uf }}{% endif %}{% if show_rank  == 'show' %}&rank{% endif %}" aria-label="Próximo">
                                <span aria-hidden="true">&gt;&gt;</span>
                            </a>
                        </li>
                    </ul>
                    <br><br>
                </nav>
            {% endif %}

        {% endif %}
    </div>

{% endblock %}
